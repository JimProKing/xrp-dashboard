[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
restartPolicyType = "on_failure"

[env]
GLOBAL_CHART_SOURCE = "auto"
XRPL_RPC_URL = "https://xrpl.ws,https://s1.ripple.com:51234,https://s2.ripple.com:51234"